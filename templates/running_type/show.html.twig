{% extends 'base.html.twig' %}

{% block title %}RunningType{% endblock %}

{% block body %}
    <h1>RunningType</h1>

<div class="container">
    <div class="d-flex justify-content-end">
        {{ include('running_type/_delete_form.html.twig') }}
    </div>
    <table class="table mt-5">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ running_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ running_type.name }}</td>
            </tr>
        </tbody>
    </table>


    <div class="d-flex justify-content-center">
        <div class="btn-group">
            <a href="{{ path('running_type_index') }}" class="btn btn-warning float-right">Retour à la liste</a>
            <a href="{{ path('running_type_edit', {'id': running_type.id}) }}" class="btn btn-success float-right">Modifier</a>
        </div>
    </div>
</div>
{% endblock %}
