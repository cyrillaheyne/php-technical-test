{% extends 'base.html.twig' %}

{% block title %}Edit RunningType{% endblock %}

{% block body %}
    <h1>Edit RunningType</h1>

    <div class="container">
        <div class="d-flex justify-content-end">
            <a class="btn btn-warning" href="{{ path('running_type_index') }}">retour à la liste</a>
        </div>
    {{ include('running_type/_form.html.twig', {'button_label': 'Update'}) }}


        {{ include('running_type/_delete_form.html.twig') }}
    </div>
{% endblock %}
