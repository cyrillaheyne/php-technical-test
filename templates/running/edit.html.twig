{% extends 'base.html.twig' %}

{% block title %}Edit Running{% endblock %}

{% block body %}
    <h1>Edit Running</h1>

    <div class="container">
        <div class="d-flex justify-content-end">
            <a class="btn btn-warning" href="{{ path('running_index') }}">retour à la liste</a>
        </div>
        {{ include('running/_form.html.twig', {'button_label': 'Update'}) }}
        {{ include('running/_delete_form.html.twig') }}
    </div>


{% endblock %}
