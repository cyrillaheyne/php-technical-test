{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <h1>User</h1>

    <div class="container">
        <div class="d-flex justify-content-end">
            {{ include('user/_delete_form.html.twig') }}
        </div>
    <table class="table mt-5">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
            </tr>
        </tbody>
    </table>


        <div class="d-flex justify-content-center">
            <div class="btn-group">
                <a href="{{ path('user_index') }}" class="btn btn-warning float-right">Retour à la liste</a>
                <a href="{{ path('user_edit', {'id': user.id}) }}" class="btn btn-success float-right">Modifier</a>
            </div>
        </div>

    </div>
{% endblock %}
