<div id="layoutSidenav_nav">
    <nav class="sb-sidenav accordion sb-sidenav-dark" id="sidenavAccordion">
        <div class="sb-sidenav-menu">
            <div class="nav">
                <a class="nav-link" href="{{ path('dashboard') }}">
                    <div class="sb-nav-link-icon"><i class="fas fa-tachometer-alt"></i></div>
                    Dashboard
                </a>
                <div class="sb-sidenav-menu-heading">Simple</div>
                <a class="nav-link" href="{{ path('user_index') }}">
                    <div class="sb-nav-link-icon"><i class="fas fa-users"></i></div>
                    Membres
                </a>
                <a class="nav-link" href="{{ path('running_type_index') }}">
                    <div class="sb-nav-link-icon"><i class="fas fa-biking"></i></div>
                    Type sorties
                </a>
                <a class="nav-link" href="{{ path('running_index') }}">
                    <div class="sb-nav-link-icon"><i class="fas fa-running"></i></div>
                    Sorties
                </a>
            </div>
        </div>
        <div class="sb-sidenav-footer">
            <div class="small">Logged in as:</div>
            {{ app.user.username }}
        </div>
    </nav>
</div>